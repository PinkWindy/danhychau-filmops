# DYC web_demo — chạy local & deploy (Render / tương tự)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
python-multipart
openpyxl>=3.1.0

# Smoke test / TestClient (tùy chọn trên CI)
httpx>=0.27.0

# Chụp screenshot báo cáo allocation (audit/capture_allocation_modal_screenshots.py):
#   pip install playwright
#   python -m playwright install chromium


google-genai
Pillow
